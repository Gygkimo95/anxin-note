# 待改

用起来发现的问题，攒在这里，改之前再一起排优先级。SPEC.md 记的是「现在是什么样、为什么」，这里记的是「哪里不对」。

---

## 已经改掉的（0.5.0）

- **排查提示教了最狠的测法**。「测一下」原来写「现在可以把 App 划掉、锁屏，等它」。但在 vivo、小米这些 ROM 上手动划掉等于强行停止，排好的定时会跟着被清掉——那是最坏情况，拿它当第一次测试，结果必然是「没弹」，用户会以为 App 坏了。改成「回桌面、锁屏等着就行，别划掉 App」，先测正常情况。
    - 真机第一次排查就踩了这个：vivo 上划掉后没弹，而「发一条」是正常的。测试条件设计错了，不是 App 坏了。
- **到点没弹这个状态会把排查结果丢掉**。原来「排查中」只显示 6 分钟，之后悄悄退回「还没记录到提醒弹出过」，等于把刚测出来的失败结论擦掉了。现在单独一个「排查那条没弹出来」状态，直说该去开什么，并且按钮留给「再测」（自启动那条就在上面，自己带按钮）。

- **提醒是会响的，而用户要的只是「弹一条提示」**。渠道建的时候用了 `IMPORTANCE_HIGH` 的默认行为，也就是响 + 震 + 横幅。给焦虑的人做的工具，突然响一声正好违反「不制造第二种压力」。改成保留 `IMPORTANCE_HIGH`（横幅要弹出来，沉在通知栏里等于没提）但把声音、震动、呼吸灯全关掉。
    - 渠道属性建完就改不动了，所以换了新 id `med-reminder-quiet` 并显式删掉旧的，不然从旧版升上来的人还是会被响。
    - **界面上一律不再说「闹钟」「响」**，改成「提醒」「弹出来」。`AlarmManager` 在代码里是叫闹钟，但那只是系统的定时器；照搬到界面上，用户读到的是「装了个会叫的东西」——这个误解真的发生了。
- **备份路径挤在按钮旁边，断行断在词中间**（「存在下载/安心手 / 记」）。路径是用户最需要照着去找的东西，移到卡片下面那段说明里，那儿有整行宽度，并且把文件名也写全了。

- **收系统广播的 receiver 写了 `exported="false"`**，`BOOT_COMPLETED` / `TIME_SET` / `MY_PACKAGE_REPLACED` 系统根本递不进来，也不报错。后果是重启或应用更新之后闹钟再也排不上；而随便打开一次 App 又会因为 `onResume` 重排而看起来正常了，所以这个故障是间歇性的、很难对上。这是「通知不自动触发」里唯一能在代码里看出来的确定 bug。
- **「闹钟响过吗」自检**。以前只能查到「权限开着、闹钟排上了」，排不到的那一段完全是黑的。现在每次闹钟真的递到 `ReminderReceiver` 就记一笔时间戳（在决定要不要弹通知之前记，因为这一顿可能已经吃过了不弹，但闹钟是响了的），设置里显示「上次 今天 08:00」。另加「测一下」排两分钟后的一条，点完可以把 App 划掉、锁屏再等它——这样就能分清是「闹钟没响」还是「响了但通知没出来」。
- **自启动入口**。国产 ROM 上到点不响最常见的原因，但系统不给任何 API 查状态，所以这条自检永远是灰点 + 一个入口，不谎报正常。入口按厂家逐个试已知的设置页组件名，全都打不开就退回应用详情页。
- **记下实际吃药的时间，并数着还多久能吃东西**。有的药吃完要空腹等一段。药上加 `waitMin`（不用等 / 半小时 / 1 小时 / 2 小时，缺省不用等），吃过的那顿在首页显示「08:12 吃的」而不是「08:00 这顿」，紧凑行显示 `✓ 08:12`。要等的话首页单独一行数分钟，按这顿里等得最久的那种药、从最后一粒起算。
    - 这一行**不能挂在某顿的卡片里**：刚记完焦点就跳到下一顿去了，挂在卡片上正好在最需要看的时候消失。第一版就是这么写的，测出来才发现。
    - 只改那一行的文字，不重画整页——正数着分钟的时候把展开状态和滚动位置弄丢特别烦人。

## 已经改掉的（0.4.1）

- **时间选择器难看**。两个 `<select>` 拼出来的东西看着是表单控件，不像在选时间。换成自己写的滚轮：
    - 折叠时只是一行大字（`08:00 早上`），点一下才展开。一天三次的药要是摊开三个滚轮，设置页就没法看了。
    - 滚轮用 CSS `scroll-snap` 吸附，停下来之后按 `scrollTop / 36` 取整算值；老 WebView 上 `scroll-snap` 不生效时在这一步自己对齐，所以不依赖它也能选准。
    - 四个常见时间做成一排 chip（早 8:00 / 午 12:00 / 晚 18:00 / 夜 22:00），常见情况一下点到，不用滚。
    - **面板必须先 `hidden = false` 再定位**：元素还在隐藏状态时 `scrollTop` 设不进去，滚轮会停在 00。所以面板是点开时才构建的，不预建。

## 已经改掉的（0.4.0）

- **一种药一天吃好几次**。数据模型升到 v3：`med.times[]`，每一次带自己的 `id` / `since` / `until`，服药记录变成 `doses[日][medId][timeId]`。要点：
    - 改时间沿用同一个 `timeId`，所以 08:00 调成 09:00 之后，已经记下的那些天不会断开变成漏服。
    - 新加的那次 `since` 从今天起，过去的日子不会凭空少一次。
    - 减掉一次用 `until`，以前记过的留在原处。今天已经吃过的那次从明天起算，不然今天那笔记录会从界面上消失。
    - 停整种药时逐个时间点分别算停止日：今天吃过的留着显示为已完成，没吃的当场消失，不会留一个「还没吃」在首页催人。

---

## 0.3.0：那次数据丢失

用户从 0.1.0 升 0.2.0 的时候先卸载再安装，一个疗程的记录全没了。卸载会删掉应用私有目录（`data.json` 就在里面），`allowBackup=false` 意味着系统里也没有副本，当时还没有导入功能。**这份数据没能救回来。**

责任在发版说明：只说了「记录会自动迁移」，没说「千万不要卸载」。覆盖安装本来是安全的（两个包签名一致，验过），用户按常规习惯先卸载完全合理。

改掉的：

- **自动备份到「下载」文件夹**，卸载删不掉。双文件轮换，并且拒绝用更空的内容覆盖已有备份。`save()` / `onResume` / 通知点「吃了」都会触发，间隔 12 小时。
- **从备份恢复**：走系统选文件；覆盖前先强制备份当前状态；导入前校验格式并跑迁移。
- **读不出来就绝对不写**。JS 侧解析失败置 `loadBroken`，`save()` 变空操作并顶一条黄条提示；原生侧 `Bridge.save` 拒绝用「没药也没记录」的状态覆盖非空文件。这两道拦的是同一个形状：读失败 → 空白兜底 → 界面像全新安装 → 用户重新填 → 空白落盘。

细节在 SPEC.md 第 6 节。

---

## 已经改掉的（0.2.0）

- **吃药时间选不准分钟**。原生 `<input type="time">` 弹的系统时钟表盘拖分钟按 5 分钟吸附，改成自己的时/分两个下拉（分钟给全 60 项）。顺带发现这个坑还连着下面那条：格式一旦不是标准 `HH:MM`，原生侧会静默取消闹钟。（0.4.1 又把那两个下拉换成了滚轮，见下。）
- **多种药**。数据模型升到 v2（`meds[]` + `doses[日][medId]` + `since`/`until`），首页按「顿」组织，一顿多药一次点击记完，需要时才展开逐个确认。详见 SPEC.md 第 3、4 节。
- **「＋ 新的方式」点了没反应**。根因是 `window.prompt()` 在没设 `WebChromeClient` 的 WebView 里不弹窗也不报错。改成页面内联输入。同一个根因还坏了「删掉这条」的二次确认（`window.confirm()`），一起修了。
- **身体反应标签可自定义**。加了「＋ 新的反应」，并在设置里做了标签管理；已被历史记录用过的标签不给删。
- **通知开启后不生效**。修了四处：通知图标改用自己的 vector（框架图标取不到会让 `notify()` 抛异常）、时间解析容错、永久拒绝权限后跳系统设置页、界面显示「下次提醒是什么时候」。**这几条都只做了代码走查，没有真机验证。**

---

## 待确认

### 提醒到底通了没有

这台开发机没有模拟器也没有 KVM，原生闹钟和通知这条链路始终没法实测。0.5.0 起排查顺序是：

1. 设置 → 提醒 → **「测一下」**，然后把 App 从后台划掉、锁屏，等两分钟。
    - 响了 → 闹钟链路是通的，回到设置能看到「排查那条在 HH:MM 响了」。
    - 没响 → 闹钟被系统拦了，去开自启动 + 电池白名单，再测一次。
2. 「发一条」如果也没动静，那是通知本身的问题（权限 / 渠道），它会把失败原因直接显示出来。
3. 两个都通但第二天早上还是没响 → 问题在「过夜」，看「闹钟响过吗」那条记的最后时间是哪一次，能定位到是哪一顿开始断的。
4. 通知上点「吃了」之后，回到 App 首页状态有没有跟着变。

**下一步（如果自启动 + 白名单都开了还是过不了夜）**：只剩常驻前台服务这条路——一条低优先级的常驻通知把进程钉住，写「下次 21:00 · 舍曲林」，顺带还能当「一眼就看到」的入口。得做成可关的，别硬塞给所有人。

### 备份写进去了没有

MediaStore 那条路径同样没法在这台机器上实测。装上 0.3.0、在设置里点一次「立即备份」之后，请到文件管理器的 `下载/安心手记/` 看看有没有 `安心手记-备份.json`。如果「立即备份」报了错，那句话就是原因。

---

## 还没提的

- **不是每天吃的药**（隔天、每周几）：`times[]` 上再加一层「哪几天」，是同一块结构里的下一步。
- **按需救援药（PRN）**：按时吃的药关心「漏没漏」，按需吃的药关心「什么时候吃的、多久一次」，是两种记录逻辑，真要动就一次想完。
- **减掉某一次之后，那天已经记下的那笔在补记页看不见**（文件里还在）。现在的规避是「今天吃过就从明天起算」，但如果是很久以前的某天有记录、而那个时间点后来被减掉，那天的补记页只列当天该吃的几次，那笔历史记录不显示。等做复诊导出的时候要一起解决：导出必须以磁盘上真实的 `doses` 为准，不能只列「现在的方案该吃几次」。
