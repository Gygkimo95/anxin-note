# 安心手记：焦虑治疗期的服药与压力记录

## 1. 定位

治疗期间自用的记录工具，只解决三件事：

1. **今天这顿药吃了没** —— 消除的是不确定感，不是做统计。
2. **压力起来的时候，把当时的情况留住** —— 当下 30 秒能完成。
3. **事后回来补：我做了什么、有没有用** —— 攒成自己的应对手册。

和「生活实践档案」是两个产品。那个明确反打卡、反提醒、必须配图；这个天生需要状态和定时提醒，原则冲突，所以独立成一个 App，只共用安卓 WebView 壳的技术路子。

## 2. 不可违反的原则

- **一眼可答**：打开就知道今天吃没吃，不用滚动、不用点进去。确认动作只要一次点击，通知上也能直接点。
- **不制造第二种压力**：不做连续天数、不做服药率百分比、不用红色、不用「漏服」「失败」「已断」这类词。没记的那天只是空着。
- **可以统计方法，不统计人**：应对方式的平均降幅要算给用户看（这是记录的回报）；服药依从率、连续天数一律不做。断掉带来的自责会让人放弃记录，甚至开始记假的。
- **压力源和处理路径是同一条记录的两段**，不是两个功能。当下只写「发生了什么 / 多难受 / 身体反应」；事后补「念头 / 做了什么 / 现在还剩多少」。第二段永远是可选的、不催的。
- **不做任何医疗判断**：漏服要不要补、剂量怎么调，只显示用户自己填的医嘱备注，App 不给建议。
- **数据不出这台手机**：不申请 INTERNET 权限，无账号，`allowBackup=false`。
- **丢数据是这个 App 最严重的故障**，比崩溃、比提醒不响都严重。看病期间攒的记录是要带去复诊的，没了补不回来。所以：备份必须自动、必须落在卸载删不掉的地方；任何写入都不许在读取失败的情况下发生。

## 3. 数据模型（v3）

单个 JSON 文件，存在应用私有目录 `files/data.json`。

```json
{
  "version": 3,
  "onboarded": true,
  "meds": [
    { "id": "m1", "name": "劳拉西泮", "dose": "0.5mg", "note": "别喝酒",
      "since": "2026-09-14", "until": "", "waitMin": 30,
      "times": [
        { "id": "t1", "at": "08:00", "since": "2026-09-14", "until": "" },
        { "id": "t2", "at": "21:00", "since": "2026-09-16", "until": "" }
      ] }
  ],
  "remind": true,
  "doses": { "2026-09-18": { "m1": { "t1": { "at": "2026-09-18T08:12:00", "source": "notification" } } } },
  "stress": [{
    "id": "s1", "at": "2026-09-18T11:15:00",
    "what": "", "level": 6, "body": ["心慌"],
    "thought": "", "methods": ["出门走走"], "after": 3, "closedAt": ""
  }],
  "methods": ["深呼吸", "出门走走"],
  "bodyTags": ["心慌", "胸闷"]
}
```

- `after === null` 表示这条还没补完，首页会给一个不催促的入口。
- **「记没记」的最小单位是 `(药, 时间点)` 这一对**，不是药。一种药一天吃三次就是三条独立的记录，所以 `doses` 是 `[日][medId][timeId]` 三层。这个键结构让重复确认天然幂等。
- **`since` / `until` 是必需的，不是可选装饰**，而且**时间点也各有一份**。某天该吃哪几次按这两层算：`since` 之前、`until` 之后都不计入（`until` 取排他，即那天起就不算了）。少了它们，加一种药会让过去所有日子凭空变成「漏了」，减掉中午那次会让往后每天都缺一格——都是白送的焦虑。停药、减次都用 `until` 标记而不是删掉，否则历史记录会看不懂。
- **改时间必须沿用同一个 `timeId`。** 08:00 调成 09:00 是同一次药挪了个钟点，不是减一次加一次；换了 id 已经记下的那些天就跟这个时间点断开，凭空变成漏服。
- 停一样东西时，**今天已经记过的那一次从明天起算，没记过的从今天起算**（`stopDay`）。这样既不会让今天记下的那笔从界面上消失，也不会在首页留一个「还没吃」催人。停整种药时逐个时间点分别算。
- **`doses[日][药][次].at` 是实际吃的时间，不是计划时间。** 有的药吃完要空腹等一段才能进食，用户看的就是这个钟点。所以吃过的那顿在首页显示「08:12 吃的」顶替「08:00 这顿」，紧凑行也显示 `✓ 08:12`。
- `waitMin` 是「吃完隔多久能吃东西」，缺省 0 表示不用等、界面上什么都不显示（大多数药不需要，不给每种药都挂个倒计时）。设了之后首页单独一行数分钟，按这顿里等得最久的那种药算，从**最后一粒**的 `at` 起算。这行不挂在任何一顿的卡片上——刚吃完焦点就跳到下一顿去了，挂在卡片里正好在最需要的时候看不见。数到了就换成「可以吃东西了」，隔天重画就不再出现。
- `bodyTags` 和 `methods` 都是用户可增删的。已经被历史记录用过的标签不允许删。
- 将来要加按需的救援药，应另开 `prn` 数组（按发生记录，不参与「漏没漏」的判断）。

### 迁移

一级一级往上升，`Store.migrate()` 和 JS 的 `migrate()` 必须同步改，两边行为一致。原生侧 `readObject()` 每次读都会跑一遍并写回；JS 侧那份是给浏览器预览路径兜底的。

- **v1 → v2**：单个 `med` 对象 + `doses[日]={at, source}`，包成 `meds:[{id:"m1", ...}]` 和 `doses[日][m1]`，`med.remind` 提到根上，`since` 取最早那条服药记录的日期。
- **v2 → v3**：`med.time` 变成 `times:[{id:"t1", at, since, until}]`（`since`/`until` 继承药本身的），`doses[日][medId]` 多包一层 `{t1: ...}`。

迁移中途抛异常就**不写回**，宁可用旧结构继续跑，也不能落一份半成品。

### 为什么原生侧持有数据

通知上点「吃了」的时候 App 大多没打开，WebView 里没有 JS 在跑，只有原生能把这条落下去。所以：

- `Native.load()` / `Native.save(json)`：网页层读写整份状态，但 **`save` 会强制保留原生文件里的 `doses`**，一次普通保存不能覆盖掉通知里记下的那次。
- `Native.setDose(day, medId, timeId)` / `Native.clearDose(day, medId, timeId)`：服药状态的唯一写入通道，返回最新的 `doses`。
- `onResume` 时回调 `window.__reloadFromNative()` 重读，保证前台看到的是通知改过之后的状态。

### 不能用 window.prompt / confirm

安卓 WebView 没有设 `WebChromeClient` 的话，`prompt()` 和 `confirm()` **既不弹窗也不报错**，按钮看起来就是「点了没反应」。所有输入和确认都必须走页面内的内联控件（`inlineInput` / `inlineConfirm`）。这不是风格偏好，是硬约束。

## 4. 页面

| 页面 | 职责 |
|------|------|
| 初次设置 | 第一种药的药名、剂量、每天几点、医嘱备注 |
| 今天（首页） | 该处理的那一顿做成大卡 + 一键确认；其余顿紧凑一行；最近 7 天；待补完入口；「有点紧张，记一下」 |
| 某天补记 | 那天该吃的每一次按顿分组，逐个点开或取消 |
| 压力速记 | 发生了什么（可空）+ 强度 0–10 + 身体反应标签（可新建） |
| 记下了 | 确认 + 把用户自己历史上有效的前 3 个方式推到面前 |
| 后来呢 | 念头 + 做了什么（方式标签，可新建）+ 现在还剩多少 |
| 应对手册 | 每个方式的使用次数与平均降幅，排序按收缩后的分数 |
| 记录 | 压力（按日倒序，显示 `7 → 4`）/ 服药（当月 + 上月日历，三态） |
| 设置 | 多种药增删改、每种药一天几次、提醒开关与权限自检、标签管理、备份与恢复、关于 |

### 多种药、一天多次，都按「顿」组织

一顿 = 同一个时间点该吃的所有 `(药, 时间点)`。用户的动作是「这顿吃完了」，不是逐个药打勾。所以：

- 首页同一时刻只把**一顿**做成大卡片，「都吃了」一次点击记完整顿，确认成本不随药的数量或次数增长。
- 点其他任意一顿，它会被顶上来当大卡片，动作依然是一次点击。
- 一顿有多条时才提供「只吃了一部分」，展开后逐个确认。默认不展开。
- 一种药一天吃好几次时，它会在好几顿里各出现一次，彼此独立。早上那次记了不影响晚上那次。
- 一天的状态是三态：`✓` 全记了 / `◐` 记了一部分 / 没记。**「记了一部分」不能用警告色**，它是事实不是错误；那天没有在吃的药则显示为灰的 `–`，不参与判断。

设置里每种药下面是一行一次的时间列表，各自能改能减，末尾一个「＋ 再加一次」。每一行折叠状态只是一行大字（`08:00 早上`），点一下才展开滚轮 + 四个常见时间的 chip，一次只开一个——一天三次的药要是同时摊开三个滚轮，这页就没法看了。**不能用 `<input type="time">`**：系统时钟表盘拖分钟按 5 分钟吸附，选不准。滚轮靠 CSS `scroll-snap` 吸附，但停下来之后仍按 `scrollTop / 行高` 取整算值并自己对齐一次，所以老 WebView 上不依赖 `scroll-snap` 也选得准。面板必须先显示出来再设 `scrollTop`，隐藏元素上设不进去。新加的那次默认排在最后一次之后 6 小时（封顶 22:00），撞上已有时间就往后挪，省得用户每次都从 08:00 拨起。减药（一天三次减到两次）在治疗过程里是常态，所以减掉单独一次必须和停整种药一样容易。

排序上做了向均值收缩：`score = 平均降幅 × 次数 / (次数 + 1)`，否则只用过一次的偶然结果会压在用过多次的上面。手册里对只有一次记录的显式标「才一次，先别当结论」。

## 5. 提醒（安卓）

**要的是「到点静静弹一条」，不是闹钟。** 这是原则「不制造第二种压力」在通知上的具体形态：给焦虑的人做的东西不该突然响一声。所以渠道保留 `IMPORTANCE_HIGH`（它决定的是横幅弹不弹出来，这条得看得见，沉在通知栏里等于没提），但 `setSound(null, null)`、`enableVibration(false)`、`enableLights(false)`。响不响和弹不弹是两件独立的事，别混。

- **渠道属性建完就改不动了**，只有用户能在系统设置里改。所以 0.5.0 把渠道 id 换成 `med-reminder-quiet` 并显式删掉旧的 `med-reminder`，否则从旧版升上来的人还是会被响。以后任何要改渠道属性的改动都得换 id + 删旧的。
- Android 7 上没有渠道，静音要写在每条通知上（`setDefaults(0).setSound(null).setVibrate(null)`）。
- **界面措辞一律不用「闹钟」「响」**，用「提醒」「弹出来」。`AlarmManager` 在代码里叫闹钟，但那只是系统的定时器，说给用户听就变成「装了个会叫的东西」——真发生过这个误解。
- 每一顿一个定时（一天几次就是几个），`AlarmManager.setExactAndAllowWhileIdle` 排下一次，到点之后重排；`SecurityException` 时退回不精确，宁可晚几分钟也别不来。
- **不同顿的 PendingIntent 必须真的互不相同**：request code 用 `时*60+分`，并且 Intent 带上不同的 data URI。只靠 extra 区分的话系统会认为是同一个 PendingIntent，多顿会互相覆盖，最后只剩一个闹钟。
- 已排过的时间点记在 SharedPreferences（不写进 `data.json`，那是用户数据）。重排时先按这份清单全部撤销再重排，否则改过时间的旧闹钟会留在系统里变成幽灵提醒。
- 开机、对时、换时区、应用更新后闹钟都会丢，`ReminderReceiver` 重新排。
- 到点前这一顿已经自己记过了，就不发这条通知。
- 通知带「吃了」／「都吃了」动作，`DoseActionReceiver` 按 slot 把那一顿的药全部写盘。
- 设置里有自检，缺哪项给按钮直接跳系统页：通知权限、准点提醒（Android 12+）、**后台不受限**、**自启动**。后两项在国产 ROM 上是硬需求，不加白名单提醒会被掐掉。
- **自启动这条永远显示灰点，不报「正常」也不报「异常」**：系统没有任何 API 能查它开没开，只能给一个入口。装作知道状态比不知道更糟。入口按厂家逐个试已知的设置页组件名（MIUI / EMUI / ColorOS / OriginOS / Flyme / 一加 / 乐视），`resolveActivity` 能解析才 start，全都打不开就退回应用详情页。
- **「提醒到过吗」是排查的落脚点**：每次定时真的递到 `ReminderReceiver` 就往 SharedPreferences 记一笔时间戳（`recordFire`，在决定要不要弹通知**之前**记——这一顿可能已经吃过了不弹，但定时是到了的）。设置里显示「上次 今天 08:00」。另有「测一下」排两分钟后的一条，用户可以划掉 App、锁屏再等它。没有这个，「到点没弹」永远只能靠猜。

### 几个让提醒静默失效的坑（都踩过）

- **通知图标不能用 `android.R.drawable.*`**：`Icon.createWithResource("android", ...)` 在部分 ROM 上取不到，`notify()` 直接抛异常，通知无声无息地没了。必须用自己的 `R.drawable.ic_notify`（纯白剪影）。
- **时间字符串不能按定长解析**：曾经要求正好 5 个字符，遇到 `8:00` 或 `08:00:00` 就当成非法，**静默取消全部闹钟**。一律用 `split(":")` 容错解析。
- **POST_NOTIFICATIONS 被永久拒绝后 `requestPermissions` 是空操作**，「去开」按钮点了毫无反应。要用 `shouldShowRequestPermissionRationale` 判断，之后直接跳系统通知设置页。
- **界面必须说出「下次提醒是什么时候」**。刚打开提醒却要等到明天才来，用户看着就是坏的。设置里显示「下次提醒 今天 08:00（每天 08:00、21:30 各一次）」。
- **收系统广播的 receiver 必须 `exported="true"`**。`BOOT_COMPLETED`、`TIME_SET`、`MY_PACKAGE_REPLACED` 是系统发过来的，receiver 不导出的话系统**根本递不进来，也不报错**。结果是重启（或应用更新）之后闹钟再也排不上，而打开一次 App 又会因为 `onResume` 重排而「看起来好了」——最难查的一类。
- **闹钟活不过强杀**。被系统或用户从后台清掉（国产 ROM 上划掉最近任务往往就是 force-stop）时，AlarmManager 里排的东西一并没了，而且没有任何回调能知道。这一条代码里解决不了，只能靠自启动 + 电池白名单，以及把「响过没响」记下来让用户能对。
- 「试一下 · 发一条」返回真实失败原因并显示出来，不许假装成功。

## 6. 备份与恢复

用户在 0.2.0 那次升级时**先卸载再安装**，一整个疗程的记录全没了。卸载会连带删掉应用私有目录，`allowBackup=false` 又意味着系统层面没有副本，当时也没有导入功能——三件事叠在一起，数据是真的找不回来。下面每一条都是那次事故的直接产物。

- **备份必须写在共享目录**：`下载/安心手记/`，通过 MediaStore（API 29+）或 `Environment.DIRECTORY_DOWNLOADS`（更早的版本）。放在应用私有目录里的快照不叫备份，它跟着卸载一起消失。
- **自动，不靠用户记得**：`save()`、`onResume`、通知上点「吃了」都会触发，间隔 12 小时。手动「立即备份」跳过间隔限制。
- **双文件轮换**：写新的 `安心手记-备份.json` 之前，先把上一份挪成 `安心手记-备份-上一次.json`。只有一个文件的话，一次坏的备份就把好的盖掉了。
- **拒绝写更空的快照**：比较 `meds + stress + doses` 的条数，比现存备份少就不自动覆盖（手动备份可以强制）。
- **恢复走系统选文件**（`ACTION_OPEN_DOCUMENT`），不自己扫目录。覆盖之前先强制备份一次当前状态，选错文件也还有退路。导入前校验：解析得出 JSON、含 `meds`/`med`/`stress`/`doses` 之一、内容非空，然后跑一遍迁移。

### 两道护栏：读不出来的时候绝对不许写

事故的形状是「读失败 → 拿空白兜底 → 界面看起来像全新安装 → 用户重新填 → 空白落盘」。所以两侧都要拦：

- **JS**：`load()` 里读到了内容却 `JSON.parse` 失败（或 `native.load()` 抛异常），置 `loadBroken`，此后 `save()` 直接 return，并顶一条黄条说明「已经停止写入，原来的东西还在原处」，落到设置页让用户去恢复。宁可什么都不能改，也不能悄悄盖掉。
- **原生**：`Bridge.save` 里，如果传进来的状态既没有 `meds` 也没有 `stress`，而磁盘上的有，就拒绝写入。界面里没有「清空全部」这种操作，所以这种保存只可能是兜底逻辑漏出来的，不是用户的意思。

### 升级时不要卸载

同一个签名的包直接覆盖安装就行，数据在私有目录里保留。发新版本给用户的时候必须说清这一句——上次没说，代价是一个月的记录。

## 7. 明确不做 / 延后

- 复诊小结导出（用户暂时不要；要做的话需要顺带记副作用和睡眠）。
- 按需救援药（PRN）。
- 云同步、账号、多设备。
- 完整 CBT 思维记录（证据、替代想法）。
- 应用锁、通知隐藏药名。
- 不是每天吃的药（隔天、每周几）。
